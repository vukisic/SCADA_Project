<UserControl x:Class="GUI.Views.ScadaDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="1000"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
                TextElement.FontWeight="Regular"
                TextElement.FontSize="13"
                TextOptions.TextFormattingMode="Ideal" 
                TextOptions.TextRenderingMode="Auto"   
                Background="{DynamicResource MaterialDesignPaper}"
                FontFamily="{DynamicResource MaterialDesignFont}">
    
    <UserControl.Resources>
        <Style TargetType="DataGridRow" x:Name="BaseStyle">
            <EventSetter Event="MouseDoubleClick" Handler="DataGrid_MouseDoubleClick" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="Points" ItemsSource="{Binding Path=Points}" SelectionMode="Single" AutoGenerateColumns="False"
				  CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False"
				  SelectionUnit="FullRow"
				  SelectionChanged="Points_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Index" Binding="{Binding Index}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Mrid" Binding="{Binding Mrid}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Value" Binding="{Binding Value}" IsReadOnly="True" Width="*" />
                <DataGridTextColumn Header="Register type" Binding="{Binding RegisterType}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding TimeStamp}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Measurement type" Binding="{Binding MeasurementType}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Alarm" Binding="{Binding Alarm}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Class type" Binding="{Binding ClassType}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Direction" Binding="{Binding Direction}" IsReadOnly="True" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>  
    </Grid>
</UserControl>
